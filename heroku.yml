setup:
  config:
    API_PORT: 3333
    REACT_APP_API_URL: http://localhost:3333
    REACT_APP_MAP_TILE_URL: https://a.tile.openstreetmap.org/{z}/{x}/{y}.png

build:
  docker:
    web:
      dockerfile: heroku.Dockerfile
